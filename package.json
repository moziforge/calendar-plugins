{
  "name": "@moziforge/calendar-plugins",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private calendar plugin family for Mozi agents: provider-specific calendar integrations exposed as Host-plane DSH tools.",
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "test": "pnpm run build && pnpm -r run test:unit"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "oxlint": "^1.12.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
