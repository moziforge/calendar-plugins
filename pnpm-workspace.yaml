packages:
  - packages/*

# pnpm denies dependency lifecycle scripts unless each package is reviewed here.
# esbuild is pulled in by vitest and builds its own platform binary.
allowBuilds:
  esbuild: true
